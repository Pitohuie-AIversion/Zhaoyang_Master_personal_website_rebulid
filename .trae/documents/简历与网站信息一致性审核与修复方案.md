## 审核范围

* 简历文件：`resume/cn_Resume_compressed-1-2.pdf`、`resume/en_Resume_compressed.pdf`

* 站点信息源：`src/locales/zh.json`、`src/locales/en.json`

* 渲染页面与组件：`src/pages/Home.tsx`、`src/pages/Contact.tsx`、`src/pages/Research.tsx`、`src/pages/Publications.tsx`、`src/pages/Projects.tsx`、`src/components/Footer.tsx`

## 已发现问题

* 简历下载链接错误：主页按钮指向`/resume.pdf`，与现有文件不匹配，且未按语言切换（`src/pages/Home.tsx:515-528`）。

* 中英文专利申请单位不一致：英文翻译中`applicant`有“Dalian University of Technology”，与中文的“西湖大学/大连海事大学”不一致（需统一到实际单位）。

* 统计数字待核对：`home.statsData`中的论文/专利/引用/项目/奖项数量需要与简历一致。

## 修复方案

1. 简历链接修复

   * 按当前语言动态指向：中文→`/resume/cn_Resume_compressed-1-2.pdf`，英文→`/resume/en_Resume_compressed.pdf`。

   * 增加存在性校验与降级方案（文件缺失时隐藏按钮或提示）。
2. 中英文专利申请单位统一

   * 以简历为准，同步`src/locales/en.json`与`src/locales/zh.json`相关键的`applicant`字段。
3. 统计数字一致化

   * 以简历为准同步`home.statsData.*`，并在`Research/Publications`页的数据列表中保持一致。
4. 社交与联系方式核验

   * 按简历核对`contact.info.*`与页脚`footer.contact.*`，保持中英文一致；若简历有更多方式（如ORCID、个人域名），补充到\`contact.social

