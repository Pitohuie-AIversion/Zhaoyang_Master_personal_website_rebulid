# 安全策略

## 报告漏洞

请通过 GitHub 的私密安全报告（Security Advisory）或仓库所有者提供的私密联系方式报告漏洞。不要在公开 Issue、提交信息或日志中披露密钥、利用代码或个人数据。

报告建议包含：

- 受影响的页面、API 或版本；
- 可复现的最小步骤与影响评估；
- 已采取的临时缓解措施；
- 安全的联系渠道。

仓库所有者应先确认收到报告，再完成分级、修复、验证和轮换可能泄露的凭据。修复发布前避免公开完整细节。

## 支持范围

仅当前生产版本和 `master` 分支接受安全修复。历史分支、备份脚本与本地实验文件不作为受支持的生产接口。

## 密钥与数据

- 浏览器端 `VITE_*` 变量视为公开信息。
- 服务角色密钥、管理员令牌、加密密钥和 OpenAI 密钥只能由部署平台的密钥管理提供。
- 发现密钥进入 Git 历史后，应立即吊销并轮换；仅删除当前文件并不足够。
- 生产数据导出、日志和备份应遵循最小权限、加密和到期删除原则。
