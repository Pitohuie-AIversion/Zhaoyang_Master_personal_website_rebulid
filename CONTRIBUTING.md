# 贡献指南

## 开始前

1. 使用 Node.js 22+ 和 npm 10+。
2. 运行 `npm ci`，从 `.env.example` 创建本地 `.env`。
3. 从 `master` 创建范围明确的分支，不要提交密钥、构建目录或本地工具缓存。

## 开发约定

- 保持改动聚焦，避免在同一个提交中混入无关格式化。
- TypeScript 新代码应保持类型清晰；不要新增无理由的 `any`。
- 用户可见文案应同步维护 `src/locales/en.json` 和 `src/locales/zh.json`。
- 修改 API、环境变量、数据库迁移或部署行为时，应同步更新文档和回滚说明。
- 数据库迁移必须是可追踪的新文件，不要改写已经投入使用的历史迁移。

提交信息建议使用 `feat:`、`fix:`、`docs:`、`test:`、`chore:` 等明确前缀。

## 提交前验证

```bash
npm run verify
npm run audit:prod
```

生产依赖审计需要联网。涉及 UI 的改动还应人工检查桌面端、移动端、键盘操作和中英文切换。

## Pull Request

PR 描述应包含问题背景、改动范围、验证证据、风险与回滚方式。CI 必须通过；覆盖率报告会作为工作流产物保留 14 天。
